-- Create first table PRODUCTS_LINES with  constraints
CREATE TABLE PRODUCT_LINES (
   line_number number(8) constraint prodLine_pk PRIMARY KEY,
   line_notes varchar(255),
   line_began date
   line_name varchar(255)
   highest_profit_item 
   highest_volume_item
   product_group_ID
);